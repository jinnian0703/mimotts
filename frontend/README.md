# Mimo Frontend

Next.js frontend for the Mimo audio operations console.

## Commands

```bash
npm run dev
npm run build
npm run lint
```

Production deployment is managed by `deploy/docker` or `deploy/source`.
